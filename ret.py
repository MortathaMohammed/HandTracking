from char import character as ch
class Return:
    def all_char(list, tipIds):
        if ch.A_a(list, tipIds):
            return "A a"
        elif ch.B_b(list, tipIds):
            return "B b"
        elif ch.C_c(list, tipIds):
            return "C c"
        elif ch.D_d(list, tipIds):
            return "D d"
        elif ch.E_e(list, tipIds):
            return "E e"
        elif ch.F_f(list, tipIds):
            return "F f"
        elif ch.G_g(list, tipIds):
            return "G g"
        elif ch.H_h(list, tipIds):
            return "H h"
        elif ch.I_i(list, tipIds):
            return "I i"
        elif ch.J_j(list, tipIds):
            return "J j"
        elif ch.K_k(list, tipIds):
            return "K k"
        elif ch.L_l(list, tipIds):
            return "L l"
        elif ch.M_m(list, tipIds):
          return "M m"
        elif ch.N_n(list, tipIds):
           return "N n"
        elif ch.O_o(list, tipIds):
            return "O o"
        elif ch.P_p(list, tipIds):
            return "P p"
        elif ch.Q_q(list, tipIds):
            return "Q q"
        elif ch.R_r(list, tipIds):
            return "R r"
        elif ch.S_s(list, tipIds):
            return "S s"
        elif ch.T_t(list, tipIds):
            return "T t"
        elif ch.V_v(list, tipIds):
            return "V v"
        elif ch.U_u(list, tipIds):
            return "U u"
        elif ch.W_w(list, tipIds):
            return "W w"
        elif ch.X_x(list, tipIds):
            return "X x"
        elif ch.Y_y(list, tipIds):
            return "Y y"
        elif ch.Z_z(list, tipIds):
            return "Z z"

       
       
       